<div class="events container">
  <% @events.each_slice(2) do |events| %>
    <div class="row">
      <% events.each do |e| %>
        <%= render 'events/event', locals: { event: e } %>
      <% end %>
    </div>
  <% end if @events %>
</div>
